import { SettingsShell } from '@/features/settings/SettingsShell'

/** Block #2 surface — fullscreen settings shell (Profile + S1–S8). */
export default function SettingsPage() {
  return <SettingsShell />
}
