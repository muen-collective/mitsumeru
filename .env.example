# Mitsu — environment template (names only; never commit real values)
# Copy to .env.local and fill, or provision via Vercel env (preferred).

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Clerk (new Clerk application "Mitsu" — web + Electron shared account layer)
# https://dashboard.clerk.com → Applications → create "Mitsu"
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
